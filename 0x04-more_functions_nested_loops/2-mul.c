#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/**
 * mul - Entry point
 *@a: parameter
 *@b: paramter
 * Return: Always 0 (Success)
 */
int mul(int a, int b)
{
return (a * b);
}
